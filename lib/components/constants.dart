import 'package:flutter/material.dart';

//Stores all the little constants used throughout the project for consistency.
//Constants start with a 'k' for better auto suggest in studio.Naming Convention.

const kIconSize = 80.0;
const kBoxHeight = 15.0;
const kFontSize = 18.0;
const kFontColor = 0xFF8D8E98;

const kLabelTextStyle =
    TextStyle(fontSize: kFontSize, color: Color(kFontColor));

const kLargeNumberTextStyle =
    TextStyle(fontSize: 50.0, fontWeight: FontWeight.w900);

const kLargeButtonTextStyle =
    TextStyle(fontSize: 25.0, fontWeight: FontWeight.bold);

const kTittleTextStyle = TextStyle(fontSize: 50.0, fontWeight: FontWeight.bold);

const kResultTextStyle = TextStyle(
    color: Color(0xFF24D876), fontWeight: FontWeight.bold, fontSize: 25.0);

const kBMITextStyle = TextStyle(
  fontSize: 100.0,
  fontWeight: FontWeight.bold,
);

const kBodyTextStyle = TextStyle(fontSize: 22.0);

const kBottomBarHeight = 80.0;
const kBottomBarColor = Color(0xFFEB1555);
const kActiveCardColor = Color(0xFF1D1E33);
const kInactiveCardColor = Color(0xFF111328);
